interface:
  display_name: "Review Fix Loop"
  short_description: "Review/fix loop to clear major blockers."
  default_prompt: "Run a review/fix subagent loop for this repo until no major issues remain."
